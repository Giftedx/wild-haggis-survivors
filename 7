Updated [src/data/variants.ts](F:/builder-runtime/edit/f9cbe038/src/data/variants.ts:121) so both unlock comments specify `BURNS_EVOLUTION_THRESHOLD (10)` and no longer mention seven evolutions.

Verification:

- Acceptance assertion passed.
- `git diff --check` passed.
- Only `src/data/variants.ts` was modified.
- `npm test` was blocked by sandbox `spawn EPERM`.
- `npm run build` was blocked by missing dependencies (`node_modules` is absent).
- Pre-existing untracked file `7` was untouched.


