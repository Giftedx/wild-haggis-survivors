# Huge Initiatives — Master Plan

**Status.** Short list. Honest flagships only. Everything else is either already shipped, not a flagship, or an idea (see `docs/DESIGN_IDEAS.md`). Full audit trail in `docs/HUGE_INITIATIVES_VERDICT.md`.

**Rule of thumb for a small team.** Run **at most one** flagship at a time. If a flagship lacks an owner, a non-goals list, and a kill criterion, it is not a flagship yet — it is an idea.

**2026-04-23 refresh.** Eight research docs now live in `docs/research/` (roguelite patterns, Scottish content ×2, game feel, music/art tech, accessibility, cultural sensitivities, narrative design). They surfaced concrete flagship candidates that earn their rows; the ten **new flagships below** each cite which research doc informed them. The research corpus is the working ground — PRs and specs should cite it.

---

## Completed this quarter

| ID | Initiative | Outcome |
|----|------------|---------|
| **R3** | **Scene complexity budget** — split `GameScene.ts` | ✅ **Shipped (2026-04-16).** 2016 → **1225 lines** across commits `b986a7f`…`5d2a13c` (R3.1–R3.7). 16 extracted modules + **88 new unit tests**. Exit criterion relaxed from "≤800" to "≤1230" mid-ladder — further reduction hits hook-literal density and requires architectural refactor (see R3a). No regressions; green across lint / 1019 vitest / tsc+vite build / Playwright e2e at every step. |
| **R3a** | **RunScoreState extraction** — 9 counters off scene, hook contracts simplified | ✅ **Shipped (2026-04-16).** 1225 → **1186 lines** in commit `240b22c`. New `src/scenes/game/RunScoreState.ts` (11 tests) holds kill/boss/gold/elite-chain/victory counters. `EnemyKillHandlerHooks`, `RunPersistenceHooks`, `RunExitHooks` each collapsed from ~10-14 counter getters/setters to one `getRunScore()` hook. Total R3+R3a: 1699 → 1186 (−513, −30%); 141 new tests across 17 modules. Unblocks W2, W66, W27, W30 — each now inherits a single-line `getRunScore` hook instead of declaring the 14-getter counter surface. |
| **W2** | **The Moor Road — multi-act campaign** | ✅ **Shipped (2026-04-16).** 3 acts, 2 pickers (A/B), 6 routes, Skip Intermissions opt-out, Chronicle route breadcrumb, full banter surface, Glesga voice pass, Playwright smoke. 1056 → **1092 tests** across the three milestones (36 new). New modules: `RunActState`, `ActIntermissionScene` + pure resolve helpers, `dispatchActComplete`. Engine extensions: `TimeManager.scheduleRealTime`, `HazardZones.spawnHealingCircle`, `SpawnSystem` elite-weight + force-spawn + enemy-HP-mult + pause-spawns, `XPSystem.setDropValueMultiplier`, `Player.refreshDashCharges`. Save schema v3 → v4 with `RunHistoryEntry.routes`. Kill-criterion playtest data deferred to live session (recorded in `docs/progress.txt`). Downstream W18 (bilingual) and W66 (Ironmoor) now inherit the act pattern. |
| **T1** | **Deterministic replay** | ✅ **Shipped (2026-04-17 / 18).** Three-phase landing (ADR-0002 + two addenda). **Phase 1** — `ReplayRecorder` + `ReplayBlob` v1 + save schema v5 + GameScene record wire behind `whs_replay_mode` flag; fixed two non-determinism blockers (HazardZones seeded via runRng, SpawnSystem.pauseSpawnsFor on game-time). **Phase 2** — `IInput` interface + `ReplayInput` DI, Chronicle ▶ Watch button, persistent HUD chip, auto-return to Chronicle on exhaustion, e2e regression. **Phase 3** — Phaser Arcade `fps: 60, fixedStep: true`; blob v2 schema (`curseKey` / `routes` / `composedStats` snapshot); save v5 → v6 with `ReplayBlobAny` union (v1 ∪ v2); GameScene playback branch consumes v2 metadata instead of live singletons; act intermission auto-resolves from recorded picks; pure determinism regression vitest. 2469 → 2499 tests across the three phases (+30). Lint / tsc / vite build / Playwright 8/8 green at every step. `Math.random()` cosmetic paths stay per `rng.ts` policy; cross-build compat stays archive-only. |
| **F1** | **Shader pipeline + Haar fog (signature visual)** | ✅ **Shipped (2026-04-24).** First Phaser 4 custom shader + the registry infrastructure every future shader slots into. Four milestones in one session on top of the Phaser 3→4 migration that shipped the day before. **D0** addendum blocks on ADR-0003 + spec + plan rebase the design from the deleted `PostFXPipeline` class onto Phaser 4's `BaseFilterShader` render-node + `Filters.Controller` split (game-config `renderNodes` map; apply via `camera.filters.internal.add(new Controller(cam))`). **M1 infra** (7 commits): `src/systems/shaders/` ships the `ShaderRegistry` (register/get/buildRenderNodesConfig), `shaderPerf` rolling-window ms probe, `sharedNoise` seeded 2-octave value-noise factory (256×256 RGBA, mulberry32-seeded), `uploadNoise` TextureManager upload path keyed `shader:noise`, and a `registerAllShaders()` entry point wired into `src/main.ts`'s game config. **M2 shader** pulls `haarFog.ts` (pure GLSL + state + uniform-apply helper) behind thin Phaser adapters `HaarFogRenderNode` (extends BaseFilterShader, wires setupUniforms/setupTextures to bind `uMainSampler2` to the shared noise) and `HaarFogController` (extends Filters.Controller, holds density/color/time state, chainable setters). BootScene uploads the noise once per game; Phaser's TextureManager handles WebGL context-lost re-upload. **M3 integration** applies haar on ActIntermissionScene — filter controller added to `camera.filters.internal`, density tweens 0→0.8 with Sine ease-out over the configured ramp-in window, `update(delta)` advances `state.time` so noise drifts frame-by-frame. Also ships `ambientHaarDensity` on BiomeDef (loch 0.2, bog 0.1, pine/heather 0) for future BiomeController integration, plus pure `haarTransition.haarDensityAt` 1s-ramp → 500ms-hold → 1s-ramp curve (12 tests). **M4 a11y + ship** adds `capHaarForA11y` deriving a photosensitivity-safe haar from existing settings (`motionScale=0` caps density at 0.4 and doubles ramp; `motionScale=0.5` interpolates to 0.7 cap + ×1.5 ramp; `reduceParticles=true` hard-caps at 0.5; stricter cap wins when both apply). Canvas-mode early-return on `renderer.type !== Phaser.WEBGL`. New e2e `haar-intermission.spec.ts` asserts HaarFogController lands, renderNode='HaarFog' resolves, controller is active, density tween fires. 3580 → **3607 vitest** (+27 across 7 shader modules); ship-gate `npm run ci:all` green 4×project Playwright matrix (58/58). Bundle index 988.74 kB → **992.69 kB** (+3.95 kB total / +1.80 kB gzip) — 4 % of the +100 KB kill criterion budget. Vendor-phaser unchanged. Follow-ups kept lightweight: BiomeController haar driver (ambient field populated, just needs a tick hook — **shipped same day as M5 polish below**), Cailleach cold-blue tint variant (HaarFogController.setColor already exposes the path). **M5 BiomeController-driven ambient haar (2026-04-24 same session):** GameScene mounts a persistent HaarFogController on the main camera at run start (WebGL-only guard, fresh instance per scene restart, drop-reference on destroy). BiomeController accepts an `onBiomeEnter(biomeId)` callback; GameScene wires it to a sine-ease-in-out density tween through `biomeHaarTarget(settings, biome)` — pure helper that reads `BIOMES[biome].ambientHaarDensity` and funnels it through `capHaarForA11y` so live motionScale + reduceParticles keep capping the ambient density too. `state.time` advances each update(delta) so noise drifts. Two new e2e scenarios: (1) GameScene camera carries the HaarFog filter for the full run — 3 browsers, (2) teleporting the player to a scanned-from-BiomeManager loch tile lifts density above 0 via the real biome-entry path — 3 browsers. 5 new pure `biomeHaar` tests. 3607 → **3612 vitest** (+5); 58 → **64 e2e** (+6 across chromium-desktop/firefox-desktop/webkit-desktop × 2 specs); bundle index 992.69 kB → **993.49 kB** (+0.80 kB / +0.25 kB gzip). The moor now literally breathes — entering a loch biome rolls a soft haar in without a curated trigger. Signature atmosphere earned per tile, every run. |
| **H1** | **Gran's Croft — hub that grows** | ✅ **Shipped (2026-04-24).** All 3 milestones in one session (21 commits `659570b → 12689cf`). M1 scaffold: procedural Gran (3-frame knitting) + hearth (4-frame flicker) sprites, CroftScene with backdrop / amber header / 4-button action column, CroftAmbientLoop pibroch-soft pad, MenuScene PLAY retitled "AWA TAE GRAN'S", GameOver `menu` button + GameScene run-exit hook route home to Croft. M2 trophy system: save schema v13 → v14 adds `bossKillCounts` + `firstRouteVisits` + `cursedVictoriesByBoss` with W2 act-gate retroactive seed (routes[0]=gordon, routes[1]=tour_bus, victory=taxman kills); CroftTrophies pure tier logic (none/first/tenth/cursed, cursed outranks kills); mantelpiece drawer with 5-boss × 4-tier trophy art (ladle/wheel/cap/helmet/ledger + enriched + cursed overlays); photo-wall with 6 route polaroids (sepia-unvisited → colour-visited with tape strip); live `bumpBossKillCount` + `bumpCursedVictoryByBoss` + `addFirstRouteVisit` from GameScene hook + ActIntermission onResolve; trophy-click Gran-voice speech bubble (2 EN+2 SCS lines per boss + empty-shelf case). M3 drove + seasonal + polish: 14 variant silhouettes along window sill (unlocked palette + gold accent ring on selected, click-to-pick writes save.selectedVariant); seasonal props drawer with Burns Night (haggis platter + whisky glass + parchment Address card + thistle bloom) wired off `getActiveSeasonalEventKey`; bookshelf click → AlmanacScene; GamepadMenuNav over actions + Enter-key Start Run shortcut. Save schema bump + 3 new bump helpers covered by tests (145 save tests, 33 new croft tests, 3554 total vitest). e2e `croft-smoke.spec.ts` runs in <500ms transition budget on all 3 desktop browsers. Bundle delta +4.26 kB gzip (well under +80 kB kill criterion). Unblocks E1 M2+ (Burns Night croft props), B1 Phase 5 (Gran-voice location), and C1 (bookshelf diegetic entry). |

---

## Next flagship

W2, W18, W66, T1 shipped. Ten new flagship candidates surfaced by the research pass (see tables below). Stakeholder selection pending. **Recommended next three** per the research (`docs/research/ROGUELITE_RESEARCH.md §Recommended First Three Specs`): **B1** (Banter Density Push — highest ROI, lowest code risk), **R1** (Relics — third progression tier), **V2** (Haggis Variants content pack). Each has a design spec drafted in `docs/superpowers/specs/`.

---

## Real flagships (pick ONE next; rest are parking lot)

Each row: one owner, one non-goals line, one kill criterion. No row graduates from this doc without those three.

| ID | Initiative | Tier | Shape of the work | Non-goals | Kill criterion |
|----|------------|------|-------------------|-----------|---------------|
| ~~**W2**~~ | ~~**The Moor Road — multi-act campaign**~~ | — | Shipped 2026-04-16 — see "Completed this quarter" row above. | — | — |
| ~~**W18**~~ | ~~**Full Scots / English bilingual ship**~~ | — | ✅ **Shipped 2026-04-18.** Phase A (`1eb5119`, 2026-04-16) landed `LOCALES` map + `setLocale` / `getLocale` + `SCS_STRINGS` empty overlay + settings `localeKey` + Settings language-cycle row. Phase B v1 (2026-04-17) populated `SCS_STRINGS` across the UI shell (menu, chronicle, deeds, settings, gameOver, pause, shop, meta-shop, act-intermission, standing-stones, ancestral-echo, hud), in-run toasts, decision moments (5 curses, 7 evolutions, 19 achievements, 6 Moor-Road routes), loadout panel, weapon/variant/permanent-upgrade/level-up-card descriptions, boss names, meta-item catalogue, captions, biomes, tutorial, plus a one-way SCS→EN orphan guard. **Phase B v2 (2026-04-18)** completes the banter pool: 294 leaf keys across generic + per-boss + per-variant + per-weapon + per-curse + per-biome + per-route sub-pools. Voice register per `feedback_voice_register` (Still Game hearth default, Limmy edge for boss warnings / low-HP / decision moments). EN→SCS banter parity guard (`i18n.locale.test.ts`) keeps future banter additions bilingual-locked. | — | — |
| ~~**W66**~~ | ~~**Ironmoor — permadeath alt mode**~~ | — | ✅ **Shipped (2026-04-16).** Full flagship live across 3 commits on top of the MVP: opt-in ceremony modal (`09715df`), separate leaderboard `bestIronmoorSeconds` + "fastest win" Chronicle suffix (`9ff2725`), chronicle-wipe-on-death + `ironmoor_wipe_toast` (`783aeef`). New pure helpers: `wipeIronmoorHistory` (save.ts), extended `formatIronmoorLine`. 1092→1130 tests (+38). | — | — |
| **W71** | **Animation rig — skeletal haggis + weather-reactive motion** | S | Replaces procedurally-drawn sprites (`BootScene.ts`) with a rig + state machine for player and key enemies. Secondary motion for mantle, whiskers, tartan. | Not a full art overhaul. Not mocap. Pixel-art soul preserved. Static sprites remain a fallback during load. | If rig inflates frame time >10% against Sept 2026 perf baseline on target devices, descope to keyframe animation only. |
| **W95** | **Thumb-zone mobile rework** | S | Portrait mode, one-thumb play, gesture input, safe-area fluency. Not a port — a native mobile posture. | Not a separate build. Not a different balance. Not a monetisation lever. | If mobile playtest session length <50% of desktop after three iteration cycles, mobile remains "supported" but not flagship. |
| ~~**T1**~~ | ~~**Deterministic replay**~~ | — | Shipped 2026-04-17 / 18 — see "Completed this quarter" row above. | — | — |
| **P3** | **Cloud saves & cross-device** | S | Real account system, conflict resolution (CRDT or LWW), encryption at rest. Current `SaveManager.ts` is localStorage-only. | No social layer. No ownership transfer. No leaderboards in v1. | If conflict-merge tests lose any user data, revert to manual export/import. |
| **W27** | **Capture & share pipeline (one surface, not three)** | A | Single pipeline for postcards, highlight clips, and still screenshots. Consolidates what earlier drafts split into W20/W27/W50/W79. | No video-editing UI. No public CDN in v1. Share is local-save-then-user-uploads. | If pipeline adds >200 KB to shipped bundle or 3% CPU during capture, cut highlights and keep postcards only. |
| **B1** | **Banter Density Push — 300–500 lines across contexts** | S | Author new lines across Gran's pool (40+), haggis inner monologue (50+), enemy flavour (100+), Cailleach (20+), Burns citational (20+), moor moments (40+), death reflections (30+), first-time-event reserved (30+), seasonal per-event (10+). EN + SCS parity. Bilingual parity guard must hold. See `docs/superpowers/specs/2026-04-23-banter-density-push-design.md`. Research: `NARRATIVE_RESEARCH.md §6.1`, `ROGUELITE_RESEARCH.md §Tier S5`. | Not a voice-acting programme. No new trigger contexts without engine review. No banter interpolation / dynamic templating. | If EN→SCS parity fence fails for >2 weeks with new lines pending translation, pause English authoring until SCS catches up. |
| ~~**H1**~~ | ~~**Gran's Croft — hub that grows**~~ | — | Shipped 2026-04-24 — see "Completed this quarter" row above. | — | — |
| **R1** | **Relics — third progression tier** | A | New item tier above weapons/passives. 3-slot cap per run. Drop from elites + bosses only. 15–20 handcrafted relics, each a run-altering rule. See `docs/superpowers/specs/2026-04-23-relics-third-tier-design.md`. Research: `ROGUELITE_RESEARCH.md §Tier A5`. | Not a relic-crafting system. No relic upgrade levels. No relic synergies as first-class system (emergent only). No active-ability relics (covered by future A2 slot flagship). | If 15-relic launch tests show build dominance (one relic picked >60% of runs), rebalance and defer launch. |
| **M1** | **Moor Road multi-node expansion** | S | Extend shipped W2 — each act becomes a 3–5 node micro-map with node types (Encounter / Shrine / Trader / Hidden / Bargain / Rest / Elite). Players see path before walking it. See `docs/superpowers/specs/2026-04-23-moor-road-nodes-design.md`. Research: `ROGUELITE_RESEARCH.md §Tier S3`. | Not a procedural dungeon. No dynamic route generation (acts have fixed node bank, nodes per run are rolled). No skip-ahead-through-nodes. | If node-path UI adds >300 ms to act-intermission transition or confuses >1 in 5 playtesters, revert to shipped single-picker W2. |
| **U1** | **Rune upgrades — rule-stack card tier** | A | New upgrade-card tier between rare and legendary. Runes are *conditional rules* not flat stats — "+100% dmg to enemies in fog", "every 10th kill spawns a healing thistle". ~30 runes v1. Stone-carved card art. See `docs/superpowers/specs/2026-04-23-rune-upgrades-design.md`. Research: `ROGUELITE_RESEARCH.md §Tier S4`, Balatro/Isaac patterns. | Not a replacement for existing card pool. No runes that chain-modify other runes in v1 (second-order emergence stays future work). No runtime authoring UI. | If Rune card pick-rate < 15% of rare+ picks after 30-run playtest, descope to ~10 most-popular and cut the rest. |
| **V2** | **Haggis Variants content pack — +3 new variants** | A | Ship three new variants building on Cailleach precedent: **Doric Quinie** (Northeast voice), **Peerie Shetlander** (Norn-tinged), **Burns's Wee Beastie** (citational poetic). Each with stat profile + palette + 24 EN / 24 SCS banter keys + unlock condition. See `docs/superpowers/specs/2026-04-23-haggis-variants-pack-design.md`. Research: `ROGUELITE_RESEARCH.md §Tier S1`, `VOICE_CARD.md` variant voices, `SCOTTISH_RESEARCH_DEEP.md §14`. | Not a ceiling shift — roster goes 10 → 13 and pauses for review. No new core mechanics per variant (stat deltas + palette + voice only). No Gaelic-only voice (all variants accessible to EN-only players). | If any variant's unlock-gate completion rate <5% over 1 month of telemetry, lower the gate rather than ship a weaker variant. |
| ~~**F1**~~ | ~~**Shader pipeline + Haar fog (signature visual)**~~ | — | Shipped 2026-04-24 — see "Completed this quarter" row above. | — | — |
| **E1** | **Seasonal events infrastructure + Burns Night (first event)** | S | Real-world-date-gated event system. First event: Burns Night (25 January ± 7 days) with haggis-piping ceremony, Address-to-a-Haggis banter thread, themed buffs, special variant-unlock path. Framework supports future events (Hogmanay, Beltane, Samhain, Up Helly Aa). See `docs/superpowers/specs/2026-04-23-seasonal-events-burns-night-design.md`. Research: `SCOTTISH_RESEARCH_DEEP.md §11.2`, `ROGUELITE_RESEARCH.md §Tier A4`. | Not a FOMO system — all event unlocks remain available year-round; only the celebration is windowed. No real-money purchase gates. No daylight-time-of-day mechanics. | If Burns Night event adds >75 KB gzip per event or conflicts with timezone-sensitive save integrity, ship as opt-in only (no auto-trigger) and revise before second event. |
| **A1** | **Accessibility foundation** | S | Fill the gaps flagged in `docs/research/ACCESSIBILITY_RESEARCH.md §Tier S`: PEAT audit entire game; colorblind audit all five tonal palettes; full key + gamepad remapping; captions scope expansion (SFX + music cues); `reduceFlashing` dedicated toggle; first-launch photosensitivity warning splash; Assist Mode scaffold (game-speed slider, extended timing, invincibility). See `docs/superpowers/specs/2026-04-23-accessibility-foundation-design.md`. | Not a blind-accessibility programme (post-1.0 stretch). No Steam accessibility tag claims until features ship. Not a screen-reader-complete menu rewrite (Phase 2). | If PEAT audit flags unfixable seizure risk in shipped VFX, mandate rebuild of that VFX — no player-facing ship until resolved. |
| **C1** | **Highland Almanac — discovery-log meta** | A | New hub scene: four books (Beasties, Weys, Finds, Banter). Every run auto-populates entries. Rare lines teased as "???". Sits between Chronicle and DeedsScene conceptually. See `docs/superpowers/specs/2026-04-23-highland-almanac-design.md`. Research: `ROGUELITE_RESEARCH.md §Tier A1`, `NARRATIVE_RESEARCH.md §6.4`. | Not achievements (DeedsScene covers that). Not run history (Chronicle covers that). No wiki import / export. | If Almanac save-data schema adds >200 KB to save size for mature players, rebuild with per-entry compression or cap entries per category. |
| **C2** | **Weapon lore pass — Dark-Souls-style item flavour** | — | *(Polish ticket, not flagship.)* Rewrite every weapon / passive / evolution / relic description in Dark-Souls-style implied-history voice. 80–120 items total. Cross-referential. See `docs/superpowers/specs/2026-04-23-weapon-lore-pass-design.md`. Research: `NARRATIVE_RESEARCH.md §3.3`. | — | — |

---

## Already shipped or near-shipped (don't "flagship" these — just finish)

The following were flagged as megaprograms in prior drafts but map to code that already exists (with tests). Treat as **polish / extension tickets**, not programs:

`R2` save migration, `R4` `as any` retirement, `C2` mutators framework (`curses.ts` + `RunModifiers`), `C3` biomes (`BiomeManager` + `BiomeController` + `BiomeRenderer`), `C4` weapon evolution (`WeaponEvolution.test.ts`), `T2` telemetry (`AnalyticsManager.ts`), `T4` perf (`SpatialCulling` + `MemoryLeak.test.ts`), `O1` a11y (`src/systems/a11y/`), `O4` i18n (`src/core/i18n.ts`), W4 banter (`banter.ts` 603 lines), **W8 post-bell** (`PostBellEscalation.ts`), W12 death literacy (`DeathCauseTracker.ts` + `deathCauseClassifier.ts`), W13 curses (`CurseScene.ts`), W15 hazards (`HazardZones.ts`), W16 juice (`JuiceSystem.ts` + `StatusFxPool.ts`), W19 tutorial (`TutorialSystem.ts`), W21 evolution (`LevelUpFlow.ts` + `evolutionChest.ts`), W22 moor moments (`moorMoments.ts`), W24 StatComposer, W25 meta purchase, **W26 auto-battle** (`src/dev/AutoBattler.ts`), W30 deeds (`AchievementManager.ts` + `DeedsScene.ts`), W31 biome crossings, W32 pre-run selection (`CurseScene` + `runStartModifiers.ts`), W34 pickups (`PickupSpawner.ts`), W35 pause (`PauseMenu.ts`), W36 debug overlay, W41 boot, W42 game over (`gameOverFormatting.ts`), W44 terrain (`highlandTerrain.ts`), **W45 Comfort** (`settingsComfort.smoke.test.ts` already exists), W46 modifiers, W47 SFX (`src/systems/audio/SFXManager.ts`), W49 event bus (`GlobalEventBus.ts`), W52 RNG (`rng.ts` + determinism tests), W56 visibility, W58 frustration, W82 "offline-first" (already true — Phaser static site + localStorage).

If one of these needs deeper authoring (banter content, more moor moments, better copy), file it as a content ticket. **Not a flagship.**

---

## Deleted outright

The following were in prior drafts and have been cut:

- **Nonsense:** A2 (WebGPU for pixel art), A4 (WASM), B1 (E2E encrypted saves in a browser survivor), G1 (monorepo for one game), G4 (plugin API), H3 (academic partnership as a product row), I-moonshots (PvP, 3D, LLM narrative), **W33** (Voronoi-explainer is a tooltip, not a flagship), **W68** (companion app is a different product), **W97** (diegetic patch notes = bad UX), **W100** ("Moor Library" is a rollup of 17 other systems and cannot legally PDF-export licensed music/VO), **M20** (enemies slowing to 60% breaks the survivors genre).
- **Duplicates:** **E2** (dupe of W13 curses), **W37** (dupe of W15 hazards), **W60** (dupe of W5 bestiary codex + O3 live ops), most of the "share surface" rows (W20/W50/W79/W90 collapsed into **W27**), most of the "hub expansion" rows (W11/W84/W85/W86/W89 collapsed into an idea entry — not yet a flagship).
- **Framing-wrong (moved to ideas):** W1 Soul Weave (an ADR, not a program), W69 A11y-as-Aesthetic (a naming exercise, needs disability consult before shipping), W80 Ethics Charter (an ADR — without an external auditor, a charter is blog copy).

---

## Parking lot (ideas, not flagships)

See `docs/DESIGN_IDEAS.md` for creative reference material that was previously dressed up as flagship rows: signature mechanics, playable roster concepts, enemy/boss bestiary sketches, biome and weapon expansion catalogues. Useful as a designer's sketchpad; **not** a prioritisation backlog.

Multiplayer (P1, W67 couch co-op), modding (T3), and store integration (PB1, PB2) stay on the parking lot until the product thesis explicitly demands them.

---

## Next steps when elevating a new flagship

Every candidate that graduates must carry:

1. **Problem statement** — player outcome + business/art outcome.
2. **Non-goals** — what v1 will not solve.
3. **Spike plan** — 1–2 weeks, exit criteria.
4. **Verification** — tests, metrics, or review gates.
5. **Dependency map** — links to sibling rows and `src/` surfaces.
6. **Kill criterion** — one falsifiable outcome that would stop the program.

Without those six, the row is an idea, not a flagship.

---

*Previous drafts bloated to 100+ W-rows, 20 M-rows, 14 H-rows, a broken scoring rubric, and ~40 synonym clusters. The audit trail is in `docs/HUGE_INITIATIVES_VERDICT.md`; the creative reference salvaged from the cut is in `docs/DESIGN_IDEAS.md`. This file stays short on purpose.*
