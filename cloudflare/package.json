{
  "name": "wild-haggis-cloud-save-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "task_08 spike — Cloudflare Worker + D1 cloud-save backend (local-only). See README.md for scope.",
  "scripts": {
    "dev": "wrangler dev --local",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240502.0",
    "miniflare": "^4.20260424.0",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.85.0"
  }
}
