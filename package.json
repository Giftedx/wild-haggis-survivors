{
  "name": "wild-haggis-survivors",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "guard:time": "node scripts/guard-time-authority.mjs",
    "test": "npm run guard:time && vitest run",
    "build": "npm run guard:time && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.60.1",
    "typescript": "^6.0.2",
    "vite": "^6.4.2",
    "vitest": "^3.2.4"
  }
}
